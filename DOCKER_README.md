# বাংলা সংবাদ পোর্টাল - Docker + PostgreSQL

## 🚀 দ্রুত শুরু করুন

### প্রয়োজনীয়তা
- Docker Desktop installed and running
- Git

### চালান
```bash
# ১. Docker containers চালু করুন
docker-compose up -d

# ২. Database seed করুন (প্রথমবার)
docker-compose exec backend node seed.js

# ৩. ব্রাউজারে খুলুন
http://localhost
```

### লগইন
- **Username:** admin
- **Password:** admin123

---

## 🏗️ আর্কিটেকচার

```
┌─────────────────────────────────────────────┐
│                  Nginx :80                   │
│         (Reverse Proxy + SSR Router)        │
└───────────────┬───────────────┬─────────────┘
                │               │
        ┌───────▼───────┐ ┌─────▼─────┐
        │  Backend :3001 │ │Frontend :80│
        │  (Express API) │ │  (React)   │
        └───────┬────────┘ └───────────┘
                │
        ┌───────▼────────┐
        │PostgreSQL :5432│
        │   (Database)   │
        └────────────────┘
```

---

## 📡 API Endpoints

### Articles
- `GET /api/articles` - সব প্রবন্ধ
- `GET /api/articles/:id` - একটি প্রবন্ধ
- `POST /api/articles` - নতুন প্রবন্ধ
- `PUT /api/articles/:id` - আপডেট
- `DELETE /api/articles/:id` - মুছুন

### Categories
- `GET /api/categories` - সব বিভাগ
- `POST /api/categories` - নতুন বিভাগ

### Auth
- `POST /api/auth/login` - লগইন
- `GET /api/auth/me` - বর্তমান ইউজার

### Webhook
- `POST /api/webhook/article` - বাইরে থেকে প্রবন্ধ যোগ

---

## 🔧 কমান্ড

```bash
# Containers চালু
docker-compose up -d

# Logs দেখুন
docker-compose logs -f

# বন্ধ করুন
docker-compose down

# সম্পূর্ণ মুছুন (database সহ)
docker-compose down -v

# Rebuild
docker-compose up -d --build
```

---

## 🌐 Social Sharing (SSR)

Facebook, Twitter, WhatsApp-এ শেয়ার করলে Nginx স্বয়ংক্রিয়ভাবে ক্রলার সনাক্ত করে SSR HTML পাঠায় যাতে সঠিক featured image দেখায়।

---

## 📁 ফাইল স্ট্রাকচার

```
├── docker-compose.yml
├── nginx.conf
├── Dockerfile (frontend)
├── server/
│   ├── Dockerfile
│   ├── index.js (Express + SSR)
│   ├── db.js (PostgreSQL connection)
│   ├── seed.js (Data migration)
│   └── routes/
│       ├── articles.js
│       ├── categories.js
│       ├── auth.js
│       ├── settings.js
│       └── webhook.js
└── src/ (React frontend)
```
